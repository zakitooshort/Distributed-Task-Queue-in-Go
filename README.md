# Distributed-Task-Queue-in-Go
A job queue system you push tasks into it, workers pick them up and execute them, failed jobs retry automatically, and a dashboard shows everything happening in real time. 
